import React from 'react';
import TopHeader from '../Inbox/TopHeader';

/**
 * Header layout component.
 * This component serves as a structural placeholder in the main layout.
 * It renders the TopHeader component which contains the actual header content,
 * including the logo, search bar, and user actions.
 */
const Header: React.FC = () => {
  return <TopHeader />;
};

export default Header;
